#!/usr/bin/env node

// const shell = require('shelljs');

// async function exec() {
//   const binPath = await initPath();
//   if (!binPath) {
//     throw new Error('jest must be installed locally or global');
//   }
//   const args = process.argv.slice(2);
//   const command = binPath + (args.length ? ' ' + args.join(' ') : '');
//   shell.exec(command);
// }
// exec();
